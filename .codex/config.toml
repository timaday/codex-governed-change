# Convenience configuration for development in this blueprint repository.
# The strict isolated reviewer is launched by a separate codex exec process.

review_model = "gpt-5.6"

[agents]
enabled = true
max_concurrent_threads_per_session = 2
default_subagent_model = "gpt-5.6"
default_subagent_reasoning_effort = "high"
