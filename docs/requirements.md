# Requirements

Status: normative MVP contract

| ID | Requirement | Acceptance evidence |
|---|---|---|
| GOV-001 | The repository MUST provide concise global/repository instruction examples and a repo-scoped governed-change skill. | Blueprint validation; skill activation tests |
| GOV-002 | Every material change profile MUST follow Context, Decide, Act, Verify, Learn. | Task-contract and workflow tests |
| GOV-003 | Evidence MUST bind to a deterministic candidate identity including relevant working-tree additions and an independently observed changed-path set; untracked regular-file bytes, executable mode and symbolic-link targets MUST be observed through retained repository directory descriptors with no-follow, type, size and post-read parent/leaf identity checks under the caller's absolute deadline, and replacement, timeout or unsupported safe access MUST block; commit mode MUST independently require actual checkout `HEAD` to equal the resolved requested head; dirty or unavailable submodule working trees MUST block; every baseline, gate, reviewer and per-mutant copied snapshot MUST be independently re-identified as the exact unmodified candidate before modification or execution while the source candidate is checked separately under the same deadline, and each gate/reviewer pre/post supplier MUST re-identify both source and copy; mutated copies MUST bind a complete concrete Git-visible tree identity that recursively includes each submodule commit and its tracked plus non-ignored untracked bytes and is derived from the verified copy rather than a separately reopened source; and admission MUST use the candidate-bound path set for protected-path authorization. | Candidate identity, deadline-bound untracked parent/leaf/symlink race, wrong-HEAD, dirty-submodule, per-copy composite source/snapshot drift, recursive-submodule and concrete-mutant-tree unit/acceptance tests |
| GOV-004 | Gate results MUST include command identity, candidate identity, exit/termination state, bounded raw artifacts, hashes, timing, truncation and limitations. | Gate runner contract tests |
| GOV-005 | Strict fresh-context review MUST run in a new ephemeral process without author conversation context and select the explicit ChatGPT-authenticated Codex model `gpt-5.6-sol`. | Reviewer command, model-identity and prompt-allowlist tests |
| GOV-006 | The fresh-context reviewer MUST have read-only candidate access, no repository write credential, and no filesystem read access beyond the sanitized harness and the minimum runtime roots required to execute tools. | Command construction, permission-profile canary and CI policy tests |
| GOV-007 | Reviewer output MUST conform to the repository schema, contain evidence-backed findings and limitations, cover the independently derived exact affected closure, and contain exactly one evidence-backed classification for every protected mandatory claim ID in the context assurance kernel. Every claim needs at least one descriptor-resolved evidence reference, and none may be `UNVERIFIED` or `UNKNOWN` when declaring `NO_BLOCKING_FINDING_OBSERVED`. | Schema, malformed-output, mandatory-claim exact-set, empty-reference, incomplete-closure and inconsistent-claim tests |
| GOV-008 | Missing, stale, timed-out, truncated, malformed, conflicting or unavailable mandatory evidence MUST become `UNKNOWN/BLOCK`. | Aggregation decision-table tests |
| GOV-009 | Any candidate mutation MUST invalidate prior gate and reviewer evidence; each untrusted gate, mutation baseline and mutant probe MUST receive a freshly reconstructed candidate copy whose own identity supplier re-identifies that exact executed copy before and after execution, so in-copy drift cannot be hidden by observing the untouched source repository and one execution cannot affect a sibling. | Copy-local pre/post drift, concurrent-copy, mutant-substitution and cross-gate mutation-isolation tests |
| GOV-010 | The Stop hook MUST force at most one continuation and then expose a blocker without looping. | Stop-hook first/continued-call tests |
| GOV-011 | CI MUST reproduce mandatory gates on an immutable commit and expose a required disposition check. Every protected gate argv MUST be self-contained in the declared clean sandbox environment; a src-layout test command MUST explicitly establish its candidate import path rather than depend on the supervisor's ambient environment. | CI static policy, exact clean-environment command and integration evidence |
| GOV-012 | Only a human MAY approve merge, deployment, risk acceptance or waiver. | Domain-policy tests and documented branch controls |
| GOV-013 | A normal candidate MUST NOT approve itself by modifying governance policy, schemas, prompts, workflows or gate definitions. | Governance-integrity and protected-policy tests |
| GOV-014 | Code changes MUST support deterministic build, test, architecture, security and risk-selected advanced gates, whose protected command descriptors execute without an undeclared ambient import path or developer environment. | Code-profile and clean-environment protected-command validation tests |
| GOV-015 | Specification changes MUST require traceability, alternatives, Three Amigos, relevant specialists, acceptance criteria, test strategy and RST review. | Specification-profile validation tests |
| GOV-016 | The reviewer MUST inspect the exact diff and every path in the independently derived affected closure while retaining full repository read access; admission MUST compare the reported closure and mandatory reviewed surfaces with the protected derivation rather than trusting the model's coverage assertion. | Reviewer prompt, protected-closure reconciliation and fixture tests |
| GOV-017 | Reviewer `PASS` MUST NOT independently create `READY_FOR_HUMAN`. | Aggregator authority tests |
| GOV-018 | Waivers MUST be explicit, human-approved, scoped, candidate-bound and expiring. | Waiver validation tests |
| GOV-019 | Working-tree mode MUST compare pre/post identities; CI MUST use immutable commit mode. | Race/drift and CI mode tests |
| GOV-020 | The reviewer launcher MUST never use `resume` and MUST record a sanitized invocation/prompt digest. | Reviewer-launcher tests |
| GOV-021 | Secrets, authentication files, author transcripts, hidden reasoning and machine-derived host values MUST NOT enter reviewer inputs or retained evidence; ChatGPT/Codex authentication MAY be available only to the parent Codex process and parent home/Codex-home/proxy values MUST NOT be inherited by model-generated tool processes, which receive only a synthetic home and non-secret runtime keys. Gate and reviewer stream normalization MUST use one shared recognizer and remove every non-empty exact discovered host value, including short hostnames, plus credential-, address-, named-endpoint- and complete absolute-host-path-shaped values before persistence; credential fields include complete multi-token authorization scheme/value forms, recognized host paths include every delimited absolute POSIX path with one or more leading separators including ordinary colon-delimited forms, complete Windows drive-user forms, and complete UNC server/share roots with or without a trailing separator or descendant, and recognized addresses include bare IPv4 and IPv6 rather than only loopback/private forms. Every such gate replacement MUST force `UNKNOWN`, and normalization MUST rescan its result so a partial replacement cannot retain another shaped value. Retained Codex JSONL MUST deterministically project command text and command output to fixed omission tokens while preserving structural completion, exit, final-message and usage facts. Actual credential values or every other non-exact ambiguous replacement outside that non-authoritative command-event projection, including final agent output, MUST force `UNKNOWN`, and normalization MUST preserve valid JSONL structure. | Shared gate/reviewer multi-token authorization, arbitrary-root, multi-leading-separator and colon-delimited POSIX/Windows/UNC-share-root path, bare IPv4/IPv6, named-endpoint, short-host and credential redaction, delimiter variants, post-normalization rescan, command-event projection, JSONL-preservation, reviewer-stream portability, parent/tool environment allowlist and security-canary tests |
| GOV-022 | MVP code MUST run on Python 3.11+ with no mandatory production dependency outside the standard library and Codex/Git executables. | Packaging and clean-environment tests |
| GOV-023 | Every disposition MUST be reconstructable from immutable referenced artifacts and producer versions; producer implementation identity MUST hash a canonical filename-, length- and digest-framed manifest of the complete trusted Python package closure; mandatory gate and mutation reconstruction MUST resolve and re-hash bounded raw streams through retained directory descriptors with no-follow, nonblocking leaf access, enforce complete non-truncated termination/status semantics, require the exact protected reviewer-prompt digest and ordered material set emitted for that gate, require capability verification no later than execution start, require provenance times to equal result times, reject a result whose start follows its end or a manifest creation time earlier than any referenced result completion, and relate each producer to its own workflow identity rather than requiring unrelated gate, mutation and reviewer runs to coincide. | Producer-closure drift, prompt/material omission/substitution/reordering, raw-stream race/tampering, capability/provenance chronology, distinct-producer-workflow and manifest reconstruction tests |
| GOV-024 | The governance system MUST include mutation/adversarial evaluation for its own fail-closed controls. The complete curated corpus bytes and digest MUST come from the authenticated previous-LKG governance source, be bound by effective policy and be copied into evidence by the protected producer; a candidate-local corpus or an ID-only match has no authority. | Protected-corpus substitution, digest, evaluation corpus and mutation gate evidence |
| GOV-025 | An unavailable or incompletely observed fresh-context reviewer MUST block; one absolute bounded monotonic deadline MUST begin before review lock/policy selection and govern every candidate, authority, policy, schema, prompt, evidence and permitted-input read, descriptor-bound launcher-closure identity read and verification, CLI-version observation, snapshot preparation, descriptor-bound candidate/evidence copy, permission finalization, Git helper, pre/post candidate observation, stdin delivery, process wait, stdout/stderr capture, cleanup, stream closure and final descriptor-bound reviewer-output read. That absolute deadline MUST cross every helper boundary unchanged and MUST NOT be converted to a remaining duration before intervening path resolution or other preparation; permission finalization MUST perform path resolution inside its bounded child, recheck the deadline immediately before launch, and derive the child timeout only from that final observation. The output schema MUST be descriptor-read once under that deadline, retained, and used directly for final result validation without a pathname reopen. Every potentially blocking copy/read MUST run in a stop-capable helper, and process execution MUST reserve enough of that same bound for forced cleanup, final identity and output materialization; both capture streams MUST reach EOF. A kernel-enforced descendant boundary whose trusted parent is not signal-addressable or resource-limit mutable from the reviewer MUST prove that no live descendant remains after every parent exit even when a descendant closes all standard streams, changes process group/session, refuses to read stdin, or attacks the same-UID supervisor through direct signal syscalls or asynchronous file-descriptor ownership, signal selection, status or notification operations made through `fcntl` or equivalent `ioctl` requests; any incomplete candidate observation or stdin delivery, missing boundary handshake or observed live descendant MUST permanently force `UNKNOWN`; and the system MUST NOT silently fall back to `/review` or a subagent. | Reviewer-unavailable, stalled initial candidate/authority/evidence/final-output/output-schema/launcher-closure read, stalled/replaced candidate, evidence, schema or launcher source, expired launcher and permission-finalization scheduler-gap deadlines, stalled Git/pre/post-observation, bounded CLI-version observation, unread-full-stdin, namespace-handshake, direct, `fcntl` and `ioctl` supervisor-assassination, resource-limit attack, retained-stream, escaped-session, liveness-race, tight elapsed-bound and bounded-cleanup process-tree tests |
| GOV-026 | The implementation MUST distinguish confirmed failure from observation uncertainty during both production and independent reconstruction; timeout, truncation, missing/tampered raw streams, incomplete observation, invalid termination, stale execution or unqualified reviewer output MUST NOT reconstruct as `PASS` or a confirmed `BLOCK`. A reviewer finding is a confirmed failure only after exact execution and qualification reconstruct; otherwise it remains advisory evidence under `UNKNOWN`. | Exit/timeout/truncation/raw-stream and stale/unqualified-finding reconstruction tests |
| GOV-027 | Commands MUST use argument arrays without shell expansion by default; shell mode MUST be explicit and risk-labelled. | Subprocess-injection tests |
| GOV-028 | Every governance CLI output MUST be resolved as a repository-relative path beneath the configured evidence root before publication; absolute paths, traversal, containment escapes, symlinks or special-file leaves, parent replacement during publication, and absence of directory-bound no-follow publication primitives MUST block without writing outside the verified directory or waiting on a special-file open. | Complete CLI output-inventory, path traversal, symlink, FIFO/special-file, parent-swap and unavailable-capability tests |
| GOV-029 | Configuration precedence and effective policy MUST be observable and digestible. | Configuration resolution tests |
| GOV-030 | The implementation MUST never perform merge, deployment, repository-setting changes, credential creation, or waiver approval. | Capability-boundary tests |
| GOV-031 | RST-inspired rapid review MUST complement deterministic checks and conformance review for both specification/design and code/change profiles; it MUST NOT replace either. | Rapid-review profile and lifecycle tests |
| GOV-032 | Risk assessments, charters, session reports, debriefs, and finding/residual-risk dispositions MUST be schema-valid, provenance-bearing, and bound to the exact candidate. | Rapid-review schema, reference, and stale-evidence tests |
| GOV-033 | Risk profiles MUST select proportionate review work: low requires a policy-valid recorded skip rationale, standard requires at least one focused charter, and elevated requires multiple relevant charters or explicit justified coverage; a candidate-supplied assessment MUST meet or exceed the protected/task risk and rapid-review floors. | Risk-profile, protected-floor and downgrade decision-table tests |
| GOV-034 | Security, authorization, destructive operations, migrations, concurrency, data integrity, public API compatibility, and governance-boundary changes MUST NOT be classified as low risk. | Hazard-escalation tests |
| GOV-035 | A rapid-review finding MUST identify the direct observation, fallible oracle, evidence linkage, threatened value, impact, severity, and confidence; checklist or heuristic completion alone MUST NOT establish success. | Session-policy and adversarial tests |
| GOV-036 | A completed rapid-review session MUST report experiments, counter-hypotheses, coverage, omissions, obstacles, follow-up work, and residual risks; blocked or inconclusive sessions MUST become `UNKNOWN`. | Session completeness tests |
| GOV-037 | Every rapid-review debrief MUST separately tell the product, testing, and quality-of-testing stories, including limitations and weak or unavailable oracles. | Debrief schema and policy tests |
| GOV-038 | The strict reviewer MUST be able to conduct a chartered rapid review in the sanitized fresh read-only harness without implementer conversation or active candidate configuration. Each rapid-review execution MUST receive and retain the exact protected permitted-input manifest, risk assessment and one approved charter and admission MUST reconstruct a one-to-one session/execution/charter binding. | Reviewer isolation, permitted-input, risk/charter substitution and one-to-one execution binding tests |
| GOV-039 | Unresolved critical/high findings and material residual risks MUST block readiness unless an authorized human supplies an exact candidate-bound disposition; a model MUST NOT accept its own residual risk. | Finding/risk disposition authority tests |
| GOV-040 | Candidate mutation MUST invalidate rapid-review artifacts and require repeated mandatory checks and focused charters before a final human-owned disposition. | Candidate-drift and lifecycle integration tests |
| GOV-041 | One protected deterministic admission kernel MUST evaluate every protected change, be the single required readiness check and the only externally visible producer of `READY_FOR_HUMAN`, fail closed for incomplete evidence, and have no merge, deploy, waiver, credential or repository-setting capability; context compilation and every other prerequisite MUST use a component-specific success state, `status` MUST remain display-only, MUST report its own state as `UNKNOWN`, MAY expose the caller-supplied value only as non-authoritative `reported_state`, and MUST NOT return success for it; candidate-local workflow code with the same check/job name MUST NOT satisfy that authority. | Reference-monitor, readiness-terminology, forged-status, candidate-impostor and capability-boundary tests |
| GOV-042 | The reference CI MUST be installed as an authority-owned organization/repository-ruleset required workflow outside the evaluated repository; its final disposition job MUST run with `if: always()`, inspect every direct prerequisite result and required artifact, fail for every non-success/absent/invalid prerequisite, use the unprefixed authority-relative admission-schema CLI contract, use SHA-pinned actions, and document an expected check source with no-bypass and current-candidate controls. | CI admission bypass, executable split-root reference invocation, authority-source and static policy tests |
| GOV-043 | The previous LKG governance source MUST explicitly define the complete TCB path set, including the trusted implementation and deployment closure, and the admission classifier MUST consume that exact protected set. Before classification, admission MUST require an authenticated protected-source LKG-policy decision that binds the exact policy digest, candidate, task and candidate base commit, and the policy's LKG commit MUST equal that base. Every schema read in admission lock selection, output preflight and evaluation MUST use an authority-relative path descriptor-read from the separately declared protected authority root and retained for the complete locked command; a candidate path, outside path, checkout-prefixed path or caller-selected alternate schema root MUST NOT supply schema authority at any admission stage. Untrusted candidate inputs—including candidate-local workflow impostors—MUST NOT redefine gates, disposition rules, reviewer instructions, schema authority, authentication rules or the TCB used to evaluate that candidate. | Authenticated LKG-policy source, protected split-root admission-schema and output-preflight, stale/base/policy substitution, complete-TCB source mutation and candidate-workflow-impostor tests |
| GOV-044 | Candidate commands MUST execute only in a disposable declared sandbox with a fresh per-gate candidate copy, no secrets, network disabled by default, bounded resources/processes/time/output, no writable protected/evidence/supervisor path, and explicit capability evidence. Each gate's protected timeout MUST begin before candidate-copy preparation and one absolute monotonic deadline MUST budget clone, checkout, submodule reconstruction, descriptor-bound stop-capable regular-file copying, copy/identity checks, container creation, command execution and cleanup; that deadline, or the earlier absolute execution deadline reserving cleanup time, MUST cross helper boundaries unchanged and MUST NOT be converted to a remaining duration that a callee rebases from a later clock observation. No provider or candidate process may start after its applicable absolute deadline. Source kind/leaf replacement, copy-helper timeout or incomplete preparation MUST retain a gate result as `UNKNOWN` without launching the command rather than escaping evidence production. A submodule copy MUST be reconstructed from its bound commit rather than copied from its working directory, so ignored or machine-local submodule files cannot enter the sandbox. The capability MUST record the pinned image and canonical command and its execution identity MUST be independently reconstructed from protected provider, image, command and every resource/time/output bound. A container-backed adapter MUST complete one bounded create/cleanup transaction, descriptor-read the supervisor-owned container-ID file with no-follow, nonblocking type/size and parent/leaf replacement checks under the same transaction deadline during both creation and cleanup, obtain and validate the immutable container ID/name pair before starting the timed command, resolve any late identity after a client timeout, forcibly remove only resolved exact IDs, quarantine an unresolved reserved name with successful all-container polling for the entire bounded cleanup interval rather than a fixed number of empty polls, and accept resolved absence only after stable successful provider queries prove both ID and exact name absent; any observed rename or same-name substitution permanently taints cleanup, and missing identity, failed create/removal, delayed creation, ambiguous provider failure, unavailable isolation or unproven cleanup MUST be `UNKNOWN/BLOCK`. | Whole-gate preparation/execution absolute-deadline propagation and scheduler-gap no-launch, stalled-copy/special-leaf and container-ID-file replacement race, retained preparation-UNKNOWN/no-launch, sandbox capability/identity tampering, exact-limit comparison, ignored-submodule exclusion, post-empty-threshold delayed-create, exact-ID timeout/removal, provider-failure, stable-absence, cleanup-time rename/substitution, copy-local mutation isolation and write/escape tests |
| GOV-045 | Candidate and evidence identity MUST include a protected stable `repository_id`; evidence from another repository MUST never validate even when Git or content digests coincide. | Repository identity and cross-repository replay tests |
| GOV-046 | Trusted evidence packaging MUST emit in-toto-shaped gate provenance and a content-addressed reviewer-execution statement binding repository/candidate subjects, task/policy/context/qualification/prompt/schema/model/launcher/environment identities, the exact executed argv digest separately from its reconstructable portable semantic argv digest, workflow attempt, tools, materials, times, limits, result, termination, primitive supervisor/capture observations, cleanup completeness, usage, complete framed producer-closure identity, and direct write-once normalized stdout/stderr references. Admission MUST resolve and re-hash those streams, parse the final Codex JSONL result and usage, and derive execution facts rather than trusting summary booleans. | Attestation schema, producer-closure identity, exact/portable argv separation, raw-stream/result/usage reconstruction, reviewer-execution observation and provenance tests |
| GOV-047 | Authoritative artifacts MUST be content-addressed and write-once across both the artifact store and every CLI output path: same-path/same-content writes are idempotent, conflicting replacement blocks, every opened leaf MUST be re-stated relative to the retained parent descriptor after bounded readback and still name the same regular-file device/inode, every self-identifier calculation has a documented exclusion rule, and every observation-through-publication command MUST acquire a non-blocking OS-backed lock on the retained no-follow evidence-root directory descriptor before output preflight and retain that same root device/inode binding through publication; a writable, hardlinkable or replaceable lock leaf is forbidden, and changed authority, root or leaf replacement, unavailable safe locking or contention is `UNKNOWN/BLOCK`. | Store/CLI idempotence, conflicting overwrite, directory-lock/root-binding interleaving, deterministic existing-leaf/publication/readback replacement, hardlink/leaf-replacement, multi-process lock and manifest identity tests |
| GOV-048 | Task approval, governance authorization, reviewer-qualification label approval, risk reduction, waiver issuance and waiver consumption MUST use protected decision-source verification plus typed references bound to their exact subject, scope, issuer and validity period; content address, model/free-text/boolean assertions and unverified issuer fields are not authority. A qualification MUST bind the verified label-decision ID and cannot infer human approval from corpus prose. Validity MUST be evaluated against a current UTC timestamp captured by the protected disposition runner, never a replayable event timestamp, use the exclusive upper bound `issued_at <= now < expires_at`, and evaluation time MUST not precede referenced manifest creation. | Missing-source-verification, authenticated-decision, label-decision forgery, trusted-current-time, exact-expiration boundary, clock-order and rerun-after-expiry tests |
| GOV-049 | Protected policy MUST derive minimum risk, gate and rapid-review obligations from changed paths, affected surfaces and the authenticated task; task/candidate input may increase but no risk assessment may reduce them, and any reduction requires an applicable authenticated waiver. | Risk-floor, rapid-review-downgrade and gate-downgrade tests |
| GOV-050 | A change touching any previous-LKG TCB path MUST be evaluated by the independently authenticated previous LKG policy. Admission MUST descriptor-resolve a typed proposed-policy reference, authenticated promotion-decision reference and rollback-evidence reference; resolve and re-hash the rollback evidence's typed gate-result, sandbox capability and provenance references; reconstruct their result, capability, raw streams, producer, chronology and policy-defined rollback-gate semantics; require the policy command argv and machine-readable successful stdout to identify the authenticated previous-LKG/base commit as the target; reject every rollback limitation; invoke the LKG-promotion predicate; and require its exact success before readiness. The protected producer MUST run this separately from the ordinary task-selected gate set using rollback code from a read-only materialization containing exactly the previous-LKG package producer manifest's paths and verified bytes, rooted at that package rather than a broader source tree, with no extra importable siblings and Python site initialization disabled; ignored, untracked and candidate-relative rollback code has no authority. It MUST bind proposed-policy and target materials and emit the nested typed rollback evidence. Missing, self-evaluated, forged, stale, digest-shaped or mismatched promotion/rollback evidence MUST block or remain `UNKNOWN`. | Admission-path old-policy replacement, authenticated base binding, promotion-reference, exact-manifest protected-package rollback producer, sibling `sitecustomize` exclusion, candidate-script substitution, actual-command/output target mismatch, nested-artifact reconstruction and missing/tampered/dummy-artifact tests |
| GOV-051 | Readiness MUST be reconstructed as a deterministic assurance case containing exactly one of each of the nine protected claim IDs, each paired with its fixed argument rule, supporting/refuting evidence, limitations and unresolved defeaters rather than accepted from an evidence bag or model narrative. | Assurance-case omission, duplication, rule-substitution and defeater tests |
| GOV-052 | Every authoritative JSON input, schema, evidence locator and reviewer-CLI/harness evidence materialization MUST be typed, repository-relative or artifact-relative as declared, digest-bound where required and resolved from one bounded byte observation through retained directory descriptors and no-follow, nonblocking regular-file reads whose parent chain and leaf name are re-stated after readback and still name the opened device/inode. Launch, qualification and admission MUST share one exact permitted-input key set per review mode: `evidence_root` is mandatory in every mode, rapid-review risk/charter keys are mandatory only in rapid review and forbidden in conformance, and the complete independently reconstructed document—not a selected subset—MUST match. An allowlisted key absent from protected evidence contracts MUST be rejected rather than silently omitted during reconstruction. One complete locked CLI command MUST cache referenced bytes by canonical repository root and normalized path with their expected digest, reject a second conflicting digest and make schema validation, lock selection, output preflight, evaluation and later command use consume the same observation rather than reopen its pathname. In a split checkout the reviewer and admission CLIs MUST use distinct declared roots: candidate evidence resolves only beneath the candidate repository, while prompt and schema bytes resolve only beneath the protected authority root; admission MUST rebuild reviewer stdin from the descriptor-read protected-authority prompt rather than any candidate path, and prefixing either argument with its checkout directory is invalid. The reviewer launcher MUST retain the output-parent descriptor before execution, read the output leaf exactly once through that descriptor, and use those identical raw bytes plus the retained deadline-bound schema bytes for parsing, schema validation, hashing, byte-count observation and write-once CLI materialization without canonical reserialization or pathname reopen. Every reviewer finding MUST name an existing canonical repository path and in-bounds line and cite a resolved repository-file or repository-excerpt locator for that same path and line. Missing, swapped, special-file, oversized or hallucinated path/line references MUST be `UNKNOWN`. | Direct and referenced authoritative JSON/schema command-session single-read/conflicting-digest, exact mode-specific permitted-input shape and full-document equality, evidence-locator and finding-location binding, split-checkout reviewer/admission command and end-to-end reviewer CLI/materialization resolution, raw noncanonical output preservation, deterministic parent/leaf swap, symlink, FIFO, oversized-replacement and tampering tests |
| GOV-053 | For a fixed authority set, adding a failure/unknown/defeater, removing required evidence, introducing staleness, or changing repository/candidate/policy MUST never improve disposition; claim labels and reviewer outcomes MUST state only bounded observation. | Exhaustive monotonicity and terminology tests |
| GOV-054 | The model lane MUST be described and implemented as fresh-context review: no author conversation/reasoning, active candidate configuration, writes, tool-visible credentials, hooks, subagents, connectors, unrelated MCPs, resume or fallback; model-generated tools MUST have network disabled, a fixed environment-key allowlist and filesystem reads restricted to the sanitized harness plus minimum runtime roots; protected instructions MUST require repository-relative tool arguments and prohibit invented machine-, endpoint- or credential-shaped values; the reviewer MUST inspect exact diff and independently derive affected closure with full repository retrieval access inside that harness. A Linux seccomp fallback MUST reject alternate-ABI syscall spaces before native dispatch, including the complete x32-tagged space on x86_64, and argument-aware deny asynchronous `fcntl` ownership, signal-selection, leases and notifications plus `F_SETFL` only when its flags include `O_ASYNC`, and equivalent `ioctl` ownership, signal-selection and async-status requests, so indirect file-descriptor signalling cannot bypass signal or cross-process-write denial while benign status changes such as `O_NONBLOCK` remain available. | Reviewer permission-profile, instruction, isolation, environment canary, alternate-ABI syscall, asynchronous-`fcntl`/`ioctl` denial plus benign-operation canary, closure and tool-boundary tests |
| GOV-055 | Every distinct review mode and material prompt/schema/model/launcher/Codex-CLI-version combination MUST have its own protected qualification identity evaluated against an exactly bound human-labelled corpus whose typed coverage classes include a seeded defect, prompt injection and clean control and whose every critical case includes a human-approved defect ID plus concrete requirement/path/line target. Corpus, label-decision and case-evidence documents MUST be typed and content-addressed. The protected adapter MUST reject a corpus missing any mandatory class or critical expected-finding target; descriptor-read the retained corpus once, reject duplicate JSON object keys, require its raw byte digest to equal the manifest reference, protected policy, qualification record and case evidence, parse and validate those same bytes, require the label-decision ID in the verified decision set and require that decision to approve the exact content-addressed corpus; deterministically reconstruct each synthetic candidate from the exact corpus file bytes; descriptor-resolve and re-hash every per-case source bundle, projection, context qualification, prepared context receipt, post-run context receipt, permitted-input manifest, normalized captured stdout/stderr stream, reviewer output and reviewer-execution statement; descriptor-read each protected qualification schema once and use those retained bytes for its digest, parse and every case validation; independently rebuild the source inventory, closure, adverse signals, selected profile, projection, both context receipts, the complete mode-specific permitted-input document, the complete semantically validated portable reviewer argv, equality between the top-level and primitive-supervisor exact executed-argv digests, and exact stdin bytes from the protected prompt plus permitted inputs; reconcile the resulting argv/stdin digests and final Codex JSONL agent message, usage and thread; derive capture, cleanup, binding and execution validity from primitive trusted observations; bind each execution to the exact case and qualification identity and, for rapid review, the exact risk assessment and one approved charter; require each conformance output to bind the reconstructed gate manifest and prepared context receipt, exactly report the protected affected closure and required reviewed surfaces, contain the complete mandatory claim-ID set, and use only independently resolvable corpus-derived evidence references; require each critical conformance result to contain a blocking finding whose requirement, path and line match its approved target, require each critical rapid-review result to additionally match the protected defect ID through its typed finding ID, require both modes' evidence to resolve to that exact corpus source; and independently recompute critical recall from those defect matches rather than from `BLOCK` dispositions, plus false pass/block, unknown, latency and aggregate qualification. A blanket blocker, empty findings, same-file/different-line finding, wrong defect ID, unrelated finding, self-reported argv/stdin/execution flags, partial or mode-invalid permitted-input/risk/charter substitution, conflicting exact argv digests, supplied digest-shaped candidate or context IDs, stale gate/context bindings, incomplete closure or reviewed surfaces, missing mandatory claims, unresolved evidence references, internally inconsistent summaries, missing case evidence, raw/canonical corpus confusion, corpus mismatch, or expired transient artifacts MUST block. Before other normalization, the trusted launcher MUST replace command text and aggregated output in every successfully parsed Codex command-execution event with fixed omission tokens while retaining structural lifecycle and exit facts. It MAY replace supervisor runtime paths and allowlisted parent-environment values with a fixed runtime token. Raw stderr, malformed JSONL, final agent messages and other unstructured streams receive no command-event projection; truncation, malformed streams, retained machine values or any unproven ambiguous value block. The MVP identities MUST use `gpt-5.6-sol` through ChatGPT/Codex authentication, and a missing, shared-across-incompatible-schemas or unqualified combination MUST block. | Mandatory seeded-defect/prompt-injection/control and exact per-mode expected-finding coverage, blanket-`BLOCK`/empty/wrong-ID/wrong-line/unrelated-finding rejection, exact raw corpus-byte and duplicate-key rejection, deterministic synthetic-candidate/context-chain, exact output/executed-and-portable-argv/stdin/full permitted-input/risk/charter binding and closure/surface/claim/reference validation, single-observation protected-schema validation, typed per-mode corpus/case/context/normalized-stream/result/primitive-observation/execution reconstruction, command-event omission and raw-stderr/final-message ambiguity attacks, authenticated exact-corpus label decision, reviewer qualification, portability, authentication-boundary and version-invalidation tests |
| GOV-056 | Protected policy MAY require a human specialist and/or genuinely diverse review for high/critical work; reviewer disagreement MUST remain visible `UNKNOWN/BLOCK`, and scheduled/major whole-repository audit is distinct from per-change affected-closure review. | Review-escalation and disagreement tests |
| GOV-057 | Operational RST MUST use task/candidate-bound risk-register, fallible-oracle, charter, session, coverage, debrief and follow-up artifacts, and observations, mutants and reviewer findings MUST update risks/charters bidirectionally without turning paperwork into correctness proof. | RST artifact, binding and feedback-loop tests |
| GOV-058 | The MVP MUST run the exact policy-digested curated semantic governance mutation corpus from the previous-LKG source in disposable, copy-identity-bound candidates after a green baseline and before final review. The documented local runner default MUST equal the protected acceptance-gate timeout, which is 600 seconds in the MVP policy, so the mandatory complete baseline is bounded without relying on a machine-specific override. For every operator, the producer MUST first run the exact selected structured-probe argv and sanitized environment against a separately verified unmodified control copy and require a complete `SURVIVED` result; it MUST expose no control/mutant or target metadata to candidate tests. It MUST then re-identify both the source and a distinct unmodified mutant copy as the exact candidate under bounded deadlines, derive the expected post-mutation tree only from the verified copy, and pass the same mutant deadline through every recursive tree/file/Git observation; control failure, drift, timeout or incomplete preparation MUST emit retained `UNKNOWN` evidence without mutation-probe credit. The corpus-selected protected command template MUST resolve deterministically to one isolated structured-probe argv; both control and mutant results plus the mutant record, sandbox capabilities and execution identities MUST bind that same expanded argv, never the unresolved template or a different command. Mutant gate/provenance execution subjects and pre/post observations MUST equal the distinct concrete mutated-source identity while the control, repository subject and exact ordered candidate material retain the original candidate identity. Every valid non-equivalent mutant must be killed only when the control survives and the mutant's exact command proves at least one selected test ran and produced one or more assertion-failure records with zero errors, skips, expected failures, or unexpected successes; multiple failing subtests from one selected test are valid causal proof. Missing/substituted controls, baseline/control command or environment disagreement, survivor, subject/material mismatch, copy drift, timeout, invalid, launch, import, discovery, harness, crash, signal, malformed-probe or unresolved-equivalence outcomes MUST block or remain `UNKNOWN`. | Protected corpus, identical per-mutant unmodified controls, metadata-branch canary, policy-bound 600-second complete baseline, per-mutant pre-patch source/copy identity and deadline, stalled tree observation, original-versus-mutated subject/material identity, concrete-tree identity, in-copy drift, exact recorded/executed command identity, structured causal-probe, curated mutation execution and outcome tests |
| GOV-059 | Risk-selected generated mutation MUST use a bounded pluggable adapter and a complete mutant record; compilation, launch, import, discovery, harness, crash, signal, unexecuted or malformed-probe outcomes MUST NOT count as killed, equivalent claims require sampled human triage, and a raw mutation percentage MUST NOT be the sole gate. | Mutation adapter, protected probe, record, raw-evidence reconstruction and classification tests |
| GOV-060 | Public schemas MUST have explicit compatibility/migration policy and tests, and timestamp plus lifecycle constraints MUST validate complete RFC 3339 values and semantic ordering at the domain boundary. Adding a required field is a breaking representation change: `effective-policy` uses `3.0.0` for the protected mutation-corpus digest; `reviewer-qualification`, `context-receipt`, `sandbox-capability`, and `provenance-statement` use `2.0.0`; `evidence-manifest` uses `3.0.0` for authenticated LKG-policy authority; `rollback-evidence` uses `2.0.0` for typed nested execution references; `reviewer-result` uses `3.0.0` for protected mandatory claim IDs and non-empty evidence; `rapid-review-session` uses `2.0.0` for typed finding path/line targets; `reviewer-qualification-cases` uses `3.0.0` for full candidate plus per-case context evidence and `4.0.0` for retained permitted-input plus mode-specific risk/charter references; `reviewer-qualification-corpus` uses `3.0.0` for mandatory typed case classes plus critical expected-finding labels; and `reviewer-execution` uses `3.0.0` for primitive observations plus direct stream references and `4.0.0` for a distinct exact executed-argv digest. Every advertised breaking transition MUST have an executable migration from its immediately preceding version, require separately protected values for facts absent from the legacy document, reconstruct any content address, reject old documents at current admission, and be schema-tested on both sides of the transition. | Complete executable migration matrix, protected-field requirements, old-document/current-admission rejection, content-address reconstruction and timestamp semantic tests |
| GOV-061 | Source identity and execution-environment/sandbox identity MUST be distinct, attested and checked independently; gate and mutation source identity MUST cover a canonical framed manifest of every file in the trusted Python package closure, and any protected package materialization MUST reproduce exactly those manifest paths and digests with no extras, so changing or injecting any material shared dependency or environment prevents incompatible evidence reuse. | Complete producer-closure/materialization and source/environment replay tests |
| GOV-062 | The MVP MUST remain standalone and portable: no HiveGate runtime/code/schema dependency and no committed developer path, username, hostname, local endpoint, credential or machine-derived configuration/evidence. Ignored or untracked submodule working-directory content MUST never be copied into a candidate sandbox. | Dependency, packaging, ignored-submodule isolation and public-portability scans |
| GOV-TOKEN-001 | The system MUST minimize model context through deterministic, risk-aware progressive disclosure without omitting the exact verified candidate changed-path inventory, a protected repository-derived conservative dependency/caller/contract/test closure, or mandatory authority, currentness, risk, evidence, failure, limitation or unknown information. The trusted compiler MUST re-identify the repository and policy, resolve typed digest-bound protected source artifacts, independently derive inventory/closure and adverse risk signals, and reject every caller-supplied source or flag mismatch before content-binding the projection. | Candidate/repository/artifact-derived context projection, forged-source/flag, closure-mismatch, retrieval and compression-preservation tests |
| GOV-TOKEN-002 | Token optimizations MUST be evaluated on representative seeded-defect and governance scenarios and MUST NOT be promoted when critical-defect recall, evidence traceability or disposition correctness materially regresses. Every projection-version/profile combination MUST bind a protected content-addressed context-qualification artifact and the effective policy's exact qualification ID; missing or mismatched qualification blocks. | Typed context-qualification, profile/version identity and quality-regression tests |
| GOV-TOKEN-003 | Every governed model invocation, including every reviewer-qualification corpus case, MUST use immutable, separately referenced source-bundle, projection, context-qualification and prepared-context-receipt artifacts and emit a separately content-addressed post-run context-execution receipt bound to the input receipt and reviewer output, with the reviewer-execution statement linking all context materials. Admission and qualification MUST descriptor-read and independently reconstruct the source inventory, repository closure, adverse-signal/profile decision, projection, source/projection identities, inclusion/exclusion reasons, token/byte/retrieval metrics and context-qualification binding. Every model-reported retrieval expansion MUST name an entry in the protected compiled retrieval index and be descriptor-read and digest-verified before finalization and again during admission; equality between two model-derived copies is not evidence. Actual CLI-reported token/cache/output usage must reconcile, and missing or unavailable source/projection/retrieval/post-run evidence MUST block. | Source-bundle/projection/prepared/final context schemas, qualification-case context determinism, retrieval-index unknown/wrong-digest/unavailable/model-only reports, dummy-digest/tampering, CLI JSONL usage and admission-reconciliation tests |
| GOV-TOKEN-004 | Insufficient mandatory context budget MUST cause deterministic escalation or `CONTEXT_BUDGET_INSUFFICIENT` with `UNKNOWN/BLOCK`, never silent truncation or inferred approval. | Context-budget escalation and fail-closed tests |

## MVP completion rule

Every requirement above is mandatory. A proposed deferral changes the MVP contract and therefore requires an explicit human-reviewed specification change, traceability update, and rationale. An implementation model may not unilaterally defer a requirement.
