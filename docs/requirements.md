# Requirements

Status: normative MVP contract

| ID | Requirement | Acceptance evidence |
|---|---|---|
| GOV-001 | The repository MUST provide concise global/repository instruction examples and a repo-scoped governed-change skill. | Blueprint validation; skill activation tests |
| GOV-002 | Every material change profile MUST follow Context, Decide, Act, Verify, Learn. | Task-contract and workflow tests |
| GOV-003 | Evidence MUST bind to a deterministic candidate identity including relevant working-tree additions and an independently observed changed-path set; commit mode MUST independently require actual checkout `HEAD` to equal the resolved requested head; dirty or unavailable submodule working trees MUST block; copied snapshots MUST be independently re-identified before and after execution; mutated copies MUST bind a complete concrete Git-visible tree identity that recursively includes each submodule commit and its tracked plus non-ignored untracked bytes; and admission MUST use the candidate-bound path set for protected-path authorization. | Candidate identity, wrong-HEAD, dirty-submodule, copied-snapshot drift, recursive-submodule and concrete-mutant-tree unit/acceptance tests |
| GOV-004 | Gate results MUST include command identity, candidate identity, exit/termination state, bounded raw artifacts, hashes, timing, truncation and limitations. | Gate runner contract tests |
| GOV-005 | Strict fresh-context review MUST run in a new ephemeral process without author conversation context and select the explicit ChatGPT-authenticated Codex model `gpt-5.6-sol`. | Reviewer command, model-identity and prompt-allowlist tests |
| GOV-006 | The fresh-context reviewer MUST have read-only candidate access, no repository write credential, and no filesystem read access beyond the sanitized harness and the minimum runtime roots required to execute tools. | Command construction, permission-profile canary and CI policy tests |
| GOV-007 | Reviewer output MUST conform to the repository schema, contain evidence-backed findings and limitations, cover the independently derived exact affected closure, and contain exactly one evidence-backed classification for every protected mandatory claim ID in the context assurance kernel. Every claim needs at least one descriptor-resolved evidence reference, and none may be `UNVERIFIED` or `UNKNOWN` when declaring `NO_BLOCKING_FINDING_OBSERVED`. | Schema, malformed-output, mandatory-claim exact-set, empty-reference, incomplete-closure and inconsistent-claim tests |
| GOV-008 | Missing, stale, timed-out, truncated, malformed, conflicting or unavailable mandatory evidence MUST become `UNKNOWN/BLOCK`. | Aggregation decision-table tests |
| GOV-009 | Any candidate mutation MUST invalidate prior gate and reviewer evidence; each untrusted gate, mutation baseline and mutant probe MUST receive a freshly reconstructed candidate copy whose own identity supplier re-identifies that exact executed copy before and after execution, so in-copy drift cannot be hidden by observing the untouched source repository and one execution cannot affect a sibling. | Copy-local pre/post drift, concurrent-copy, mutant-substitution and cross-gate mutation-isolation tests |
| GOV-010 | The Stop hook MUST force at most one continuation and then expose a blocker without looping. | Stop-hook first/continued-call tests |
| GOV-011 | CI MUST reproduce mandatory gates on an immutable commit and expose a required disposition check. | CI static policy tests and integration evidence |
| GOV-012 | Only a human MAY approve merge, deployment, risk acceptance or waiver. | Domain-policy tests and documented branch controls |
| GOV-013 | A normal candidate MUST NOT approve itself by modifying governance policy, schemas, prompts, workflows or gate definitions. | Governance-integrity and protected-policy tests |
| GOV-014 | Code changes MUST support deterministic build, test, architecture, security and risk-selected advanced gates. | Code-profile validation tests |
| GOV-015 | Specification changes MUST require traceability, alternatives, Three Amigos, relevant specialists, acceptance criteria, test strategy and RST review. | Specification-profile validation tests |
| GOV-016 | The reviewer MUST inspect the exact diff and every path in the independently derived affected closure while retaining full repository read access; admission MUST compare the reported closure and mandatory reviewed surfaces with the protected derivation rather than trusting the model's coverage assertion. | Reviewer prompt, protected-closure reconciliation and fixture tests |
| GOV-017 | Reviewer `PASS` MUST NOT independently create `READY_FOR_HUMAN`. | Aggregator authority tests |
| GOV-018 | Waivers MUST be explicit, human-approved, scoped, candidate-bound and expiring. | Waiver validation tests |
| GOV-019 | Working-tree mode MUST compare pre/post identities; CI MUST use immutable commit mode. | Race/drift and CI mode tests |
| GOV-020 | The reviewer launcher MUST never use `resume` and MUST record a sanitized invocation/prompt digest. | Reviewer-launcher tests |
| GOV-021 | Secrets, authentication files, author transcripts, hidden reasoning and machine-derived host values MUST NOT enter reviewer inputs or retained evidence; ChatGPT/Codex authentication MAY be available only to the parent Codex process and parent home/Codex-home/proxy values MUST NOT be inherited by model-generated tool processes, which receive only a synthetic home and non-secret runtime keys. Reviewer stream normalization MUST remove exact parent/runtime values and credential-, endpoint- and host-path-shaped values before persistence. An endpoint- or host-path-shaped value in a Codex command-execution event may be classified as a portable source literal only when its exact decoded bytes already occur in the immutable candidate or protected reviewer inputs; an exact immutable assignment-shaped source expression whose right-hand side is a same-name method call MAY be classified likewise because it is syntax rather than a credential value. Each permitted literal MUST be replaced by a fixed source-literal token. Actual credential values, final agent output and every other non-exact ambiguous replacement MUST force `UNKNOWN`, and normalization MUST preserve valid JSONL structure. | Redaction, source-expression/source-literal provenance, JSONL-preservation, reviewer-stream portability, parent/tool environment allowlist and security-canary tests |
| GOV-022 | MVP code MUST run on Python 3.11+ with no mandatory production dependency outside the standard library and Codex/Git executables. | Packaging and clean-environment tests |
| GOV-023 | Every disposition MUST be reconstructable from immutable referenced artifacts and producer versions; producer implementation identity MUST hash a canonical filename-, length- and digest-framed manifest of the complete trusted Python package closure; mandatory gate and mutation reconstruction MUST resolve and re-hash bounded raw streams through retained directory descriptors with no-follow, nonblocking leaf access, enforce complete non-truncated termination/status semantics, require capability verification no later than execution start, require provenance times to equal result times, reject a result whose start follows its end or a manifest creation time earlier than any referenced result completion, and relate each producer to its own workflow identity rather than requiring unrelated gate, mutation and reviewer runs to coincide. | Producer-closure drift, raw-stream race/tampering, capability/provenance chronology, distinct-producer-workflow and manifest reconstruction tests |
| GOV-024 | The governance system MUST include mutation/adversarial evaluation for its own fail-closed controls. The complete curated corpus bytes and digest MUST come from the authenticated previous-LKG governance source, be bound by effective policy and be copied into evidence by the protected producer; a candidate-local corpus or an ID-only match has no authority. | Protected-corpus substitution, digest, evaluation corpus and mutation gate evidence |
| GOV-025 | An unavailable or incompletely observed fresh-context reviewer MUST block; snapshot preparation, every Git helper, pre/post candidate observation, stdin delivery, process waiting, stdout/stderr capture, cleanup and stream closure MUST share one absolute bounded monotonic deadline, with process execution reserving enough of that same bound for forced cleanup; both capture streams MUST reach EOF; a kernel-enforced descendant boundary whose trusted parent is not signal-addressable or resource-limit mutable from the reviewer MUST prove that no live descendant remains after every parent exit even when a descendant closes all standard streams, changes process group/session, refuses to read stdin, or attacks the same-UID supervisor; any incomplete candidate observation or stdin delivery, missing boundary handshake or observed live descendant MUST permanently force `UNKNOWN`; and the system MUST NOT silently fall back to `/review` or a subagent. | Reviewer-unavailable, stalled Git/pre/post-observation, unread-full-stdin, namespace-handshake, supervisor-assassination, resource-limit attack, retained-stream, escaped-session, liveness-race, tight elapsed-bound and bounded-cleanup process-tree tests |
| GOV-026 | The implementation MUST distinguish confirmed failure from observation uncertainty during both production and independent reconstruction; timeout, truncation, missing/tampered raw streams, incomplete observation or invalid termination MUST NOT reconstruct as `PASS`. | Exit/timeout/truncation/raw-stream reconstruction tests |
| GOV-027 | Commands MUST use argument arrays without shell expansion by default; shell mode MUST be explicit and risk-labelled. | Subprocess-injection tests |
| GOV-028 | Every governance CLI output MUST be resolved as a repository-relative path beneath the configured evidence root before publication; absolute paths, traversal, containment escapes, symlinks or special-file leaves, parent replacement during publication, and absence of directory-bound no-follow publication primitives MUST block without writing outside the verified directory or waiting on a special-file open. | Complete CLI output-inventory, path traversal, symlink, FIFO/special-file, parent-swap and unavailable-capability tests |
| GOV-029 | Configuration precedence and effective policy MUST be observable and digestible. | Configuration resolution tests |
| GOV-030 | The implementation MUST never perform merge, deployment, repository-setting changes, credential creation, or waiver approval. | Capability-boundary tests |
| GOV-031 | RST-inspired rapid review MUST complement deterministic checks and conformance review for both specification/design and code/change profiles; it MUST NOT replace either. | Rapid-review profile and lifecycle tests |
| GOV-032 | Risk assessments, charters, session reports, debriefs, and finding/residual-risk dispositions MUST be schema-valid, provenance-bearing, and bound to the exact candidate. | Rapid-review schema, reference, and stale-evidence tests |
| GOV-033 | Risk profiles MUST select proportionate review work: low requires a policy-valid recorded skip rationale, standard requires at least one focused charter, and elevated requires multiple relevant charters or explicit justified coverage; a candidate-supplied assessment MUST meet or exceed the protected/task risk and rapid-review floors. | Risk-profile, protected-floor and downgrade decision-table tests |
| GOV-034 | Security, authorization, destructive operations, migrations, concurrency, data integrity, public API compatibility, and governance-boundary changes MUST NOT be classified as low risk. | Hazard-escalation tests |
| GOV-035 | A rapid-review finding MUST identify the direct observation, fallible oracle, evidence linkage, threatened value, impact, severity, and confidence; checklist or heuristic completion alone MUST NOT establish success. | Session-policy and adversarial tests |
| GOV-036 | A completed rapid-review session MUST report experiments, counter-hypotheses, coverage, omissions, obstacles, follow-up work, and residual risks; blocked or inconclusive sessions MUST become `UNKNOWN`. | Session completeness tests |
| GOV-037 | Every rapid-review debrief MUST separately tell the product, testing, and quality-of-testing stories, including limitations and weak or unavailable oracles. | Debrief schema and policy tests |
| GOV-038 | The strict reviewer MUST be able to conduct a chartered rapid review in the sanitized fresh read-only harness without implementer conversation or active candidate configuration. | Reviewer isolation and permitted-input tests |
| GOV-039 | Unresolved critical/high findings and material residual risks MUST block readiness unless an authorized human supplies an exact candidate-bound disposition; a model MUST NOT accept its own residual risk. | Finding/risk disposition authority tests |
| GOV-040 | Candidate mutation MUST invalidate rapid-review artifacts and require repeated mandatory checks and focused charters before a final human-owned disposition. | Candidate-drift and lifecycle integration tests |
| GOV-041 | One protected deterministic admission kernel MUST evaluate every protected change, be the single required readiness check and the only externally visible producer of `READY_FOR_HUMAN`, fail closed for incomplete evidence, and have no merge, deploy, waiver, credential or repository-setting capability; context compilation and every other prerequisite MUST use a component-specific success state, and candidate-local workflow code with the same check/job name MUST NOT satisfy that authority. | Reference-monitor, readiness-terminology, candidate-impostor and capability-boundary tests |
| GOV-042 | The reference CI MUST be installed as an authority-owned organization/repository-ruleset required workflow outside the evaluated repository; its final disposition job MUST run with `if: always()`, inspect every direct prerequisite result and required artifact, fail for every non-success/absent/invalid prerequisite, use SHA-pinned actions, and document an expected check source with no-bypass and current-candidate controls. | CI admission bypass, authority-source and static policy tests |
| GOV-043 | The previous LKG governance source MUST explicitly define the complete TCB path set, including the trusted implementation and deployment closure, and the admission classifier MUST consume that exact protected set. Before classification, admission MUST require an authenticated protected-source LKG-policy decision that binds the exact policy digest, candidate, task and candidate base commit, and the policy's LKG commit MUST equal that base. Untrusted candidate inputs—including candidate-local workflow impostors—MUST NOT redefine gates, disposition rules, reviewer instructions, schema authority, authentication rules or the TCB used to evaluate that candidate. | Authenticated LKG-policy source, stale/base/policy substitution, complete-TCB source mutation and candidate-workflow-impostor tests |
| GOV-044 | Candidate commands MUST execute only in a disposable declared sandbox with a fresh per-gate candidate copy, no secrets, network disabled by default, bounded resources/processes/time/output, no writable protected/evidence/supervisor path, and explicit capability evidence. A submodule copy MUST be reconstructed from its bound commit rather than copied from its working directory, so ignored or machine-local submodule files cannot enter the sandbox. The capability MUST record the pinned image and canonical command and its execution identity MUST be independently reconstructed from protected provider, image, command and every resource/time/output bound. A container-backed adapter MUST complete one bounded create/cleanup transaction, obtain and validate the supervisor-owned immutable container ID/name pair before starting the timed command, resolve any late identity after a client timeout, forcibly remove only resolved exact IDs, quarantine an unresolved reserved name with successful all-container polling for the entire bounded cleanup interval rather than a fixed number of empty polls, and accept resolved absence only after stable successful provider queries prove both ID and exact name absent; any observed rename or same-name substitution permanently taints cleanup, and missing identity, failed create/removal, delayed creation, ambiguous provider failure, unavailable isolation or unproven cleanup MUST be `UNKNOWN/BLOCK`. | Sandbox capability/identity tampering, exact-limit comparison, ignored-submodule exclusion, post-empty-threshold delayed-create, exact-ID timeout/removal, provider-failure, stable-absence, cleanup-time rename/substitution, copy-local mutation isolation and write/escape tests |
| GOV-045 | Candidate and evidence identity MUST include a protected stable `repository_id`; evidence from another repository MUST never validate even when Git or content digests coincide. | Repository identity and cross-repository replay tests |
| GOV-046 | Trusted evidence packaging MUST emit in-toto-shaped gate provenance and a content-addressed reviewer-execution statement binding repository/candidate subjects, task/policy/context/qualification/prompt/schema/model/launcher/environment identities, workflow attempt, tools, materials, times, limits, result, termination, primitive supervisor/capture observations, cleanup completeness, usage, complete framed producer-closure identity, and direct write-once normalized stdout/stderr references. Admission MUST resolve and re-hash those streams, parse the final Codex JSONL result and usage, and derive execution facts rather than trusting summary booleans. | Attestation schema, producer-closure identity, raw-stream/result/usage reconstruction, reviewer-execution observation and provenance tests |
| GOV-047 | Authoritative artifacts MUST be content-addressed and write-once across both the artifact store and every CLI output path: same-path/same-content writes are idempotent, conflicting replacement blocks, every self-identifier calculation has a documented exclusion rule, and every observation-through-publication command MUST acquire a non-blocking OS-backed lock on the retained no-follow evidence-root directory descriptor before output preflight and retain that same root device/inode binding through publication; a writable, hardlinkable or replaceable lock leaf is forbidden, and changed authority, root replacement, unavailable safe locking or contention is `UNKNOWN/BLOCK`. | Store/CLI idempotence, conflicting overwrite, directory-lock/root-binding interleaving, hardlink/leaf-replacement, multi-process lock and manifest identity tests |
| GOV-048 | Task approval, governance authorization, reviewer-qualification label approval, risk reduction, waiver issuance and waiver consumption MUST use protected decision-source verification plus typed references bound to their exact subject, scope, issuer and validity period; content address, model/free-text/boolean assertions and unverified issuer fields are not authority. A qualification MUST bind the verified label-decision ID and cannot infer human approval from corpus prose. Validity MUST be evaluated against a current UTC timestamp captured by the protected disposition runner, never a replayable event timestamp, and evaluation time MUST not precede referenced manifest creation. | Missing-source-verification, authenticated-decision, label-decision forgery, trusted-current-time, clock-order and rerun-after-expiry tests |
| GOV-049 | Protected policy MUST derive minimum risk, gate and rapid-review obligations from changed paths, affected surfaces and the authenticated task; task/candidate input may increase but no risk assessment may reduce them, and any reduction requires an applicable authenticated waiver. | Risk-floor, rapid-review-downgrade and gate-downgrade tests |
| GOV-050 | A change touching any previous-LKG TCB path MUST be evaluated by the independently authenticated previous LKG policy. Admission MUST descriptor-resolve a typed proposed-policy reference, authenticated promotion-decision reference and rollback-evidence reference; resolve and re-hash the rollback evidence's typed gate-result, sandbox capability and provenance references; reconstruct their result, capability, raw streams, producer, chronology and policy-defined rollback-gate semantics; require the policy command argv and machine-readable successful stdout to identify the authenticated previous-LKG/base commit as the target; reject every rollback limitation; invoke the LKG-promotion predicate; and require its exact success before readiness. The protected producer MUST run this separately from the ordinary task-selected gate set using rollback code from a read-only materialization containing exactly the previous-LKG package producer manifest's paths and verified bytes, rooted at that package rather than a broader source tree, with no extra importable siblings and Python site initialization disabled; ignored, untracked and candidate-relative rollback code has no authority. It MUST bind proposed-policy and target materials and emit the nested typed rollback evidence. Missing, self-evaluated, forged, stale, digest-shaped or mismatched promotion/rollback evidence MUST block or remain `UNKNOWN`. | Admission-path old-policy replacement, authenticated base binding, promotion-reference, exact-manifest protected-package rollback producer, sibling `sitecustomize` exclusion, candidate-script substitution, actual-command/output target mismatch, nested-artifact reconstruction and missing/tampered/dummy-artifact tests |
| GOV-051 | Readiness MUST be reconstructed as a deterministic assurance case of claims, fixed argument rules, supporting/refuting evidence, limitations and unresolved defeaters rather than accepted from an evidence bag or model narrative. | Assurance-case rule and defeater tests |
| GOV-052 | Every authoritative evidence locator and every reviewer-CLI/harness evidence materialization MUST be typed, repository-relative or artifact-relative as declared, digest-bound and resolvable to the exact protected artifact/excerpt through retained directory descriptors and no-follow, nonblocking, bounded regular-file reads. In a split checkout the reviewer CLI MUST use distinct declared roots: candidate evidence resolves only beneath the candidate repository, while prompt and schema bytes resolve only beneath the protected authority root; prefixing either argument with its checkout directory is invalid. The reviewer CLI MUST validate, hash and materialize the same single-read retained bytes rather than reopening a pathname; missing, swapped, special-file or hallucinated path/line references MUST be `UNKNOWN`. | Evidence-locator, split-checkout reviewer command and end-to-end reviewer CLI/materialization resolution, parent/leaf swap, symlink, FIFO, oversized-replacement, single-read and tampering tests |
| GOV-053 | For a fixed authority set, adding a failure/unknown/defeater, removing required evidence, introducing staleness, or changing repository/candidate/policy MUST never improve disposition; claim labels and reviewer outcomes MUST state only bounded observation. | Exhaustive monotonicity and terminology tests |
| GOV-054 | The model lane MUST be described and implemented as fresh-context review: no author conversation/reasoning, active candidate configuration, writes, tool-visible credentials, hooks, subagents, connectors, unrelated MCPs, resume or fallback; model-generated tools MUST have network disabled, a fixed environment-key allowlist and filesystem reads restricted to the sanitized harness plus minimum runtime roots; the reviewer MUST inspect exact diff and independently derive affected closure with full repository retrieval access inside that harness. A Linux seccomp fallback MUST reject alternate-ABI syscall spaces before native dispatch, including the complete x32-tagged space on x86_64, so alternate encodings cannot bypass signal or cross-process-write denial. | Reviewer permission-profile, isolation, environment canary, alternate-ABI syscall, closure and tool-boundary tests |
| GOV-055 | Every distinct review mode and material prompt/schema/model/launcher/Codex-CLI-version combination MUST have its own protected qualification identity evaluated against an exactly bound human-labelled corpus whose typed coverage classes include a seeded defect, prompt injection and clean control. Corpus, label-decision and case-evidence documents MUST be typed and content-addressed. The protected adapter MUST reject a corpus missing any mandatory class; descriptor-read the retained corpus once, reject duplicate JSON object keys, require its raw byte digest to equal the manifest reference, protected policy, qualification record and case evidence, parse and validate those same bytes, require the label-decision ID in the verified decision set; deterministically reconstruct each synthetic candidate from the exact corpus file bytes; descriptor-resolve and re-hash every per-case source bundle, projection, context qualification, prepared context receipt, post-run context receipt, normalized captured stdout/stderr stream, reviewer output and reviewer-execution statement; independently rebuild the source inventory, closure, adverse signals, selected profile, projection and both context receipts; reconcile the final Codex JSONL agent message, usage and thread; derive capture, cleanup, binding and execution validity from primitive trusted observations; bind each execution to the exact case and qualification identity; and independently recompute critical recall, false pass/block, unknown, latency and aggregate qualification. Self-reported execution flags, supplied digest-shaped candidate or context IDs, internally inconsistent summaries, missing case evidence, raw/canonical corpus confusion, corpus mismatch, or expired transient artifacts MUST block. The trusted normalization may replace supervisor runtime paths and allowlisted parent-environment values with a fixed runtime token and exact candidate/protected-input literals with a distinct fixed source token before parsing and hashing. A source-literal exception MUST use a bounded grammar and MUST first reject any nested credential-shaped argument; truncation, malformed streams, retained machine values or any unproven ambiguous value block. The MVP identities MUST use `gpt-5.6-sol` through ChatGPT/Codex authentication, and a missing, shared-across-incompatible-schemas or unqualified combination MUST block. | Mandatory seeded-defect/prompt-injection/control coverage, exact raw corpus-byte and duplicate-key rejection, deterministic synthetic-candidate/context-chain, typed per-mode corpus/case/context/normalized-stream/result/primitive-observation/execution reconstruction, nested-credential source-literal attacks, authenticated label-decision, reviewer qualification, portability, authentication-boundary and version-invalidation tests |
| GOV-056 | Protected policy MAY require a human specialist and/or genuinely diverse review for high/critical work; reviewer disagreement MUST remain visible `UNKNOWN/BLOCK`, and scheduled/major whole-repository audit is distinct from per-change affected-closure review. | Review-escalation and disagreement tests |
| GOV-057 | Operational RST MUST use task/candidate-bound risk-register, fallible-oracle, charter, session, coverage, debrief and follow-up artifacts, and observations, mutants and reviewer findings MUST update risks/charters bidirectionally without turning paperwork into correctness proof. | RST artifact, binding and feedback-loop tests |
| GOV-058 | The MVP MUST run the exact policy-digested curated semantic governance mutation corpus from the previous-LKG source in disposable, copy-identity-bound candidates after a green baseline and before final review. The corpus-selected protected command template MUST resolve deterministically to one isolated structured-probe argv; the mutant record, gate result, sandbox capability and execution identity MUST all bind that exact expanded argv, never the unresolved template or a different command. Every valid non-equivalent mutant must be killed only when that exact command proves at least one selected test ran and ended exclusively in an assertion failure. Survivor, copy drift, timeout, invalid, launch, import, discovery, harness, crash, signal, malformed-probe or unresolved-equivalence outcomes MUST block or remain `UNKNOWN`. | Protected corpus, concrete-tree identity, in-copy drift, exact recorded/executed command identity, structured causal-probe, curated mutation execution and outcome tests |
| GOV-059 | Risk-selected generated mutation MUST use a bounded pluggable adapter and a complete mutant record; compilation, launch, import, discovery, harness, crash, signal, unexecuted or malformed-probe outcomes MUST NOT count as killed, equivalent claims require sampled human triage, and a raw mutation percentage MUST NOT be the sole gate. | Mutation adapter, protected probe, record, raw-evidence reconstruction and classification tests |
| GOV-060 | Public schemas MUST have explicit compatibility/migration policy and tests, and timestamp plus lifecycle constraints MUST validate complete RFC 3339 values and semantic ordering at the domain boundary. Adding a required field is a breaking representation change: `effective-policy` uses `3.0.0` for the protected mutation-corpus digest; `reviewer-qualification`, `context-receipt`, `sandbox-capability`, and `provenance-statement` use `2.0.0`; `evidence-manifest` uses `3.0.0` for authenticated LKG-policy authority; `rollback-evidence` uses `2.0.0` for typed nested execution references; `reviewer-result` uses `3.0.0` for protected mandatory claim IDs and non-empty evidence; `reviewer-qualification-cases` uses `3.0.0` for full candidate plus per-case context evidence; `reviewer-qualification-corpus` uses `2.0.0` for mandatory typed case classes; and `reviewer-execution` uses `3.0.0` for primitive observations plus direct stream references. Every advertised breaking transition MUST have an executable migration from its immediately preceding version, require separately protected values for facts absent from the legacy document, reconstruct any content address, reject old documents at current admission, and be schema-tested on both sides of the transition. | Complete executable migration matrix, protected-field requirements, old-document/current-admission rejection, content-address reconstruction and timestamp semantic tests |
| GOV-061 | Source identity and execution-environment/sandbox identity MUST be distinct, attested and checked independently; gate and mutation source identity MUST cover a canonical framed manifest of every file in the trusted Python package closure, and any protected package materialization MUST reproduce exactly those manifest paths and digests with no extras, so changing or injecting any material shared dependency or environment prevents incompatible evidence reuse. | Complete producer-closure/materialization and source/environment replay tests |
| GOV-062 | The MVP MUST remain standalone and portable: no HiveGate runtime/code/schema dependency and no committed developer path, username, hostname, local endpoint, credential or machine-derived configuration/evidence. Ignored or untracked submodule working-directory content MUST never be copied into a candidate sandbox. | Dependency, packaging, ignored-submodule isolation and public-portability scans |
| GOV-TOKEN-001 | The system MUST minimize model context through deterministic, risk-aware progressive disclosure without omitting the exact verified candidate changed-path inventory, a protected repository-derived conservative dependency/caller/contract/test closure, or mandatory authority, currentness, risk, evidence, failure, limitation or unknown information. The trusted compiler MUST re-identify the repository and policy, resolve typed digest-bound protected source artifacts, independently derive inventory/closure and adverse risk signals, and reject every caller-supplied source or flag mismatch before content-binding the projection. | Candidate/repository/artifact-derived context projection, forged-source/flag, closure-mismatch, retrieval and compression-preservation tests |
| GOV-TOKEN-002 | Token optimizations MUST be evaluated on representative seeded-defect and governance scenarios and MUST NOT be promoted when critical-defect recall, evidence traceability or disposition correctness materially regresses. Every projection-version/profile combination MUST bind a protected content-addressed context-qualification artifact and the effective policy's exact qualification ID; missing or mismatched qualification blocks. | Typed context-qualification, profile/version identity and quality-regression tests |
| GOV-TOKEN-003 | Every governed model invocation, including every reviewer-qualification corpus case, MUST use immutable, separately referenced source-bundle, projection, context-qualification and prepared-context-receipt artifacts and emit a separately content-addressed post-run context-execution receipt bound to the input receipt and reviewer output, with the reviewer-execution statement linking all context materials. Admission and qualification MUST descriptor-read and independently reconstruct the source inventory, repository closure, adverse-signal/profile decision, projection, source/projection identities, inclusion/exclusion reasons, token/byte/retrieval metrics and context-qualification binding. Every model-reported retrieval expansion MUST name an entry in the protected compiled retrieval index and be descriptor-read and digest-verified before finalization and again during admission; equality between two model-derived copies is not evidence. Actual CLI-reported token/cache/output usage must reconcile, and missing or unavailable source/projection/retrieval/post-run evidence MUST block. | Source-bundle/projection/prepared/final context schemas, qualification-case context determinism, retrieval-index unknown/wrong-digest/unavailable/model-only reports, dummy-digest/tampering, CLI JSONL usage and admission-reconciliation tests |
| GOV-TOKEN-004 | Insufficient mandatory context budget MUST cause deterministic escalation or `CONTEXT_BUDGET_INSUFFICIENT` with `UNKNOWN/BLOCK`, never silent truncation or inferred approval. | Context-budget escalation and fail-closed tests |

## MVP completion rule

Every requirement above is mandatory. A proposed deferral changes the MVP contract and therefore requires an explicit human-reviewed specification change, traceability update, and rationale. An implementation model may not unilaterally defer a requirement.
