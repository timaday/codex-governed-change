"""Command-line adapter skeleton."""

from collections.abc import Sequence


def main(argv: Sequence[str] | None = None) -> int:
    """Run the governance CLI after T07 implements its use cases."""
    del argv
    raise NotImplementedError("T07: implement the governed-change CLI")
